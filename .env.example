# Contact form — Resend (https://resend.com)
# Required in production for real delivery. Never commit real keys.
RESEND_API_KEY=

# Optional. Defaults to "BR Tech <onboarding@resend.dev>" (Resend test sender).
# After domain verification in Resend: "BR Tech <contact@brtech.ch>"
CONTACT_FROM_EMAIL=

# Optional override of the recipient (defaults to site.email = info@brtech.ch)
CONTACT_TO_EMAIL=
